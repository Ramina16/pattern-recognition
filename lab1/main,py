import asyncio
from functions.first_task import first
from functions.second_task import second

if __name__ == '__main__':
    asyncio.get_event_loop().run_until_complete(first())
    asyncio.get_event_loop().run_until_complete(second())
